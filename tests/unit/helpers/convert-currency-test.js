import { convertCurrency } from 'white-light/helpers/convert-currency';
import { module, test } from 'qunit';

module('Unit | Helper | convert currency');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = convertCurrency([42]);
  assert.ok(result);
});
