import { paginate } from 'white-light/helpers/paginate';
import { module, test } from 'qunit';

module('Unit | Helper | paginate');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = paginate([42]);
  assert.ok(result);
});
