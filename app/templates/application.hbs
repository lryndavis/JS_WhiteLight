<div class="container">
  <div class="navbar">
    {{#link-to 'index'}}<h1>Shop</h1>{{/link-to}}
    {{#link-to 'cart'}}<span class="cart-link">Your Cart</span>{{/link-to}}
  </div>
</div>
<div class="container">
  {{outlet}}
</div>
